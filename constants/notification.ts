import { ToastOptions } from "react-native-toast-notifications";

export const NOTIFICATION_CONFIG: ToastOptions = {
  placement: "top",
  duration: 3000,
  animationType: "slide-in",
  animationDuration: 300,
  swipeEnabled: true,
  
}
